# ============================================================================
# mml-companion-notes
# Зависимости проекта
# ============================================================================

# --- Основные библиотеки --------------------------------------------------
numpy>=1.21
scipy>=1.7
matplotlib>=3.5
sympy>=1.10
pandas>=1.3

# --- Среда Jupyter --------------------------------------------------------
jupyter>=1.0
notebook>=6.4

# --- Машинное обучение ----------------------------------------------------
# scikit-learn: используется в примерах по PCA, регрессии, нейросетям.
scikit-learn>=1.0

# --- Глубокое обучение (только в отдельных примерах) ----------------------
# torch: используется в примере 04 (свёрточный слой).
torch>=1.10

# --- Тестирование ---------------------------------------------------------
# pytest: для запуска тестов из каталога tests/.
pytest>=7.0
